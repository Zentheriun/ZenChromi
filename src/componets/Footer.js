// Footer.js
const Footer = (() => {
    const init = () => {
        // No hay lógica interactiva en el footer original.
        // Aquí iría si, por ejemplo, quisieras añadir un botón de "volver arriba" o algo similar.
    };

    return {
        init,
    };
})();

export default Footer;