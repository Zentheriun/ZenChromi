// Header.js
const Header = (() => {
    const init = () => {
        // No hay mucha lógica interactiva en el header original, pero aquí iría
        // Por ejemplo, si los elementos del menú tuvieran dropdowns o lógica de navegación SPA
    };

    return {
        init,
    };
})();

export default Header;